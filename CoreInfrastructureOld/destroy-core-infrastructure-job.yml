parameters:
- name: name
- name: dependsOn
  type: object
  default: []

jobs:
- template: ../PipelineTemplates/Jobs/Terraform/terraform-destroy-job.yml
  parameters:
    name: ${{ parameters.name }}
    workingDirectory: '$(System.DefaultWorkingDirectory)/CoreInfrastructure/Terraform'
    backendAzureRmContainerName: 'shared-core-tf-storage'
    dependsOn: ${{ parameters.dependsOn }}
